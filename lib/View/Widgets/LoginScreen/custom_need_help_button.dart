import 'package:flutter/material.dart';

class CustomLoginNeedHelpButton extends StatelessWidget {
  const CustomLoginNeedHelpButton({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TextButton(
      onPressed: () {},
      child: const Text('احتاج مساعدة'),
    );
  }
}
